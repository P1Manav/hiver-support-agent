# Core dependencies (CPU-safe — works on local machine without GPU)
# Install with: pip install -r requirements.txt

# ── Data ──────────────────────────────────────────────────────────────────────
pandas>=2.0.0
pyarrow>=14.0.0
kaggle>=1.6.0               # Kaggle API for dataset download

# ── Embeddings & Clustering ───────────────────────────────────────────────────
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0            # CPU FAISS (swap to faiss-gpu on Colab if needed)
scikit-learn>=1.4.0         # KMeans, LogisticRegression, TF-IDF, metrics
hdbscan>=0.8.38             # Optional: HDBSCAN clustering

# ── NLP & Text Processing ─────────────────────────────────────────────────────
transformers>=4.40.0        # DistilBERT tokenizer + model
datasets>=2.19.0            # HuggingFace datasets for fine-tuning pipeline
nltk>=3.8.1
spacy>=3.7.0                # PII NER (optional, for beyond-regex redaction)
# python -m spacy download en_core_web_sm  (run separately after install)

# ── Ollama client ─────────────────────────────────────────────────────────────
ollama>=0.2.0               # Python client for local Ollama models

# ── Evaluation ────────────────────────────────────────────────────────────────
scipy>=1.12.0               # Cohen's kappa (scipy.stats.cohen_kappa_score)
matplotlib>=3.8.0           # Calibration plots
seaborn>=0.13.0
tabulate>=0.9.0             # Pretty-print tables in terminal

# ── Config & Utilities ────────────────────────────────────────────────────────
pyyaml>=6.0.1
tqdm>=4.66.0
python-dotenv>=1.0.0
rich>=13.7.0                # Rich terminal output (progress bars, tables)
click>=8.1.7                # CLI argument parsing for scripts
numpy>=1.26.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-cov>=5.0.0
